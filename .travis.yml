sudo: required
services:
  - docker

before_install:
  - docker build -t eyehwan/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run eyehwan/react-test npm test -- --coverage

after_success:
  - docker build -t eyehwan/multi-client ./client
  - docker build -t eyehwan/multi-nginx ./nginx
  - docker build -t eyehwan/multi-server ./server
  - docker build -t eyehwan/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push eyehwan/multi-client
  - docker push eyehwan/multi-nginx
  - docker push eyehwan/multi-server
  - docker push eyehwan/multi-worker


